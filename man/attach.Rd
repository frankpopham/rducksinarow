% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/attach_duck.R
\name{attach_duck}
\alias{attach_duck}
\alias{attached_ducks}
\alias{detach_duck}
\alias{change_default_duck}
\alias{default_duck}
\title{Attach and manage duckdb databases / ducklakes}
\usage{
attach_duck(
  connection = NULL,
  filename,
  alias,
  read_only = FALSE,
  encrypt = FALSE,
  what = "database",
  type = "duckdb",
  parquet_encrypt = FALSE,
  parquet_directory = "data_files",
  override_parquet_directory = FALSE
)

attached_ducks(connection)

detach_duck(connection, alias)

change_default_duck(connection, new_default)

default_duck(connection)
}
\arguments{
\item{connection}{a duckdb connection. If NULL (the default), creates a connection in the temporary directory}

\item{filename}{the filename of an existing (or new) duckdb database  or ducklake (required)}

\item{alias}{an alias for the database or ducklake (required)}

\item{read_only}{default is FALSE}

\item{encrypt}{set to TRUE if the database or ducklake catalog is encrypted (or you want to encrypt if creating a new one), will ask for your password (key)}

\item{what}{Do you want to create or attach to a database or a ducklake}

\item{type}{For a ducklake do you want to use duckdb (the default) as catalog database or sqlite.}

\item{parquet_encrypt}{Only applies if creating a new ducklake, if TRUE parquet files that hold the data in the ducklake are encrypted}

\item{parquet_directory}{Only needs setting if creating a new ducklake. give a directory name for parquet files (will create directory if doesn't exist)}

\item{override_parquet_directory}{For an existing ducklake want to use an alternative parquet directory. Default is FALSE}

\item{new_default}{The alias of the new default}
}
\value{
The duckdb connection with the database or ducklake attached. Should be assigned if connection is NULL.

`attached_ducks()` returns a dataframe with the attached database and lake names

`detach_duck()` detaches and returns remaining attached databases or ducklakes

`change_default_duck()` Sets a database or ducklake as the new default

`default_duck()` returns the name of the default database.
}
\description{
Attach to an existing, or create a new, duckdb database or ducklake (`attach_duck()`)

Use `attached_ducks()` to see attached databases or lakes and `detach_duck()` to detach.

Use `default_duck()` to check current default database / ducklake and `change_default_duck()` to change the default
}
\examples{

tempfilename <- tempfile(pattern = "example_duckdb", fileext = ".duckdb")

my_con <- attach_duck(filename = tempfilename,
                alias = "example1",
                what = "database")

tempfilename_2 <- tempfile(pattern = "example_duckdb", fileext = ".ducklake")

attach_duck(connection = my_con,
                filename = tempfilename_2,
                alias = "example2",
                what = "ducklake")

attached_ducks(my_con)

default_duck(my_con)

change_default_duck(my_con, "example1")

detach_duck(my_con, "example2")

attached_ducks(my_con)

DBI::dbDisconnect(my_con)
}
